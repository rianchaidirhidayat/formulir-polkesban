{
  "entities": {
    "Form": {
      "title": "Form",
      "description": "Form schema configuration and settings",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier of the form"
        },
        "title": {
          "type": "string",
          "description": "Title of the form"
        },
        "description": {
          "type": "string",
          "description": "Description and instructions"
        },
        "questions": {
          "type": "array",
          "description": "List of questions in the form"
        },
        "theme": {
          "type": "object",
          "description": "Theme visual styling configuration"
        },
        "integrations": {
          "type": "object",
          "description": "Settings for external integrations"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of last update"
        }
      },
      "required": ["id", "title"]
    },
    "Response": {
      "title": "Response",
      "description": "Individual employee or respondent submission",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique response ID"
        },
        "formId": {
          "type": "string",
          "description": "ID of the form submitted"
        },
        "submittedAt": {
          "type": "string",
          "description": "ISO timestamp of submission"
        },
        "respondentName": {
          "type": "string",
          "description": "Name of the respondent"
        },
        "respondentEmail": {
          "type": "string",
          "description": "Email of the respondent"
        },
        "answers": {
          "type": "object",
          "description": "Key-value map of question IDs to answer values"
        },
        "googleSheetStatus": {
          "type": "string",
          "enum": ["pending", "synced", "failed"]
        },
        "emailNotificationStatus": {
          "type": "string",
          "enum": ["pending", "sent", "failed"]
        },
        "webhookStatus": {
          "type": "string",
          "enum": ["pending", "sent", "failed", "not_configured"]
        }
      },
      "required": ["id", "formId", "submittedAt"]
    },
    "Employee": {
      "title": "Employee",
      "description": "Master data pegawai for NIP lookup and auto-fill",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier, usually NIP"
        },
        "nip": {
          "type": "string",
          "description": "Nomor Induk Pegawai (NIP)"
        },
        "nama": {
          "type": "string",
          "description": "Nama lengkap pegawai beserta gelar"
        },
        "jabatan": {
          "type": "string",
          "description": "Jabatan fungsional atau struktural"
        },
        "unitKerja": {
          "type": "string",
          "description": "Unit kerja, divisi, atau jurusan"
        },
        "email": {
          "type": "string",
          "description": "Alamat email resmi pegawai"
        },
        "phone": {
          "type": "string",
          "description": "Nomor kontak atau WhatsApp"
        }
      },
      "required": ["id", "nip", "nama"]
    }
  },
  "firestore": {
    "/forms/{formId}": {
      "schema": "Form",
      "description": "Stores form definitions and configuration"
    },
    "/responses/{responseId}": {
      "schema": "Response",
      "description": "Stores form responses submitted by employees"
    },
    "/employees/{employeeId}": {
      "schema": "Employee",
      "description": "Stores master employee directory for auto-populating form fields via NIP"
    }
  }
}
